{
  "scripts": {
    "lint": "abaplint",
    "downport": "rm -rf downport && cp src -r downport && abaplint --fix abaplint_downport.json"
  },
  "devDependencies": {
    "@abaplint/cli": "^2.79.18",
    "@abaplint/runtime": "^1.6.38",
    "@abaplint/transpiler-cli": "^1.6.38"
  }
}
